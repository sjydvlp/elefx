package com.sjydvlp.elefx.component.transfer;

/** Determines how newly transferred entries are ordered in the target panel. */
public enum EleFXTransferTargetOrder {
    ORIGINAL,
    PUSH,
    UNSHIFT
}
