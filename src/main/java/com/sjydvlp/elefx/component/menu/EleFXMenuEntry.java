package com.sjydvlp.elefx.component.menu;

import javafx.scene.Node;

/** Common contract for nodes which can be placed in an {@link EleFXMenu} or submenu. */
public interface EleFXMenuEntry {

    Node node();
}
