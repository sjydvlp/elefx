package com.sjydvlp.elefx.component.pagination;

/** Type-safe pagination layout parts. */
public enum EleFXPaginationLayout {

    PREV,
    PAGER,
    NEXT,
    JUMPER,
    TOTAL,
    SIZES,
    SLOT,
    /** Pushes all following layout parts to the right edge. */
    RIGHT
}
